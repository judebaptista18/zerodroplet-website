import {NextResponse} from 'next/server';import OpenAI from 'openai';import {z} from 'zod';
const schema=z.object({message:z.string().min(1).max(1000)});const knowledge=`Zero Droplet Engineers and Consultants is based in Margao, Goa and serves South India. It designs, supplies, installs and maintains water and wastewater treatment plants. Services include RO, softening, iron removal, DM plants, STP, MBR, SBR, ETP, ZLD, DAF, ozonation, sludge handling, process monitoring, automation, annual maintenance and consultancy. The assistant must not provide a binding quotation or guarantee compliance. It should gather location, capacity, application and contact details, and direct users to the contact form for engineering assessment.`;
export async function POST(req:Request){try{const{message}=schema.parse(await req.json());if(!process.env.OPENAI_API_KEY)return NextResponse.json({answer:'Thanks for your question. Please share your location, required capacity, application and contact details through our enquiry form so an engineer can assess it.'});const client=new OpenAI({apiKey:process.env.OPENAI_API_KEY});const response=await client.responses.create({model:process.env.OPENAI_MODEL||'gpt-4.1-mini',instructions:`You are the Zero Droplet website assistant. Use only this business context: ${knowledge} Keep answers concise. Never invent prices, certifications or project claims.`,input:message,max_output_tokens:250});return NextResponse.json({answer:response.output_text})}catch{return NextResponse.json({error:'Invalid request'},{status:400})}}
